{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/troughgraph/master/assets/schema_input.json",
    "title": "nf-core/troughgraph pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "dem": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.tif$",
                "errorMessage": "TIF file for digital elevation model (DEM) must be provided, cannot contain spaces and must have extension '.tif'"
            },
            "name": {
                "type": "string",
                "pattern": "^\\S*$",
                "errorMessage": "DEM name must be provided and cannot contain spaces"
            }
        },
        "required": ["dem", "name"]
    }
}
